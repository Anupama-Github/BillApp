<h1>Listing bills</h1>
<%= form_for bills_path do |f| %>
  <%= f.autocomplete_field :code, autocomplete_bill_code_bills_path %>
<% end %>
<table>
  <tr>
    <th>Code</th>
    <th>Particulars</th>
    <th>Items</th>
    <th>Rate</th>
    <th>Amt</th>
    <th>Vat</th>
    <th>Vatamt</th>
    <th>Amount</th>
    <th>Balance</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @bills.each do |bill| %>
  <tr>
    <td><%= bill.code %></td>
    <td><%= bill.Particulars %></td>
    <td><%= bill.items.id %></td>
    <td><%= bill.rate %></td>
    <td><%= bill.amt %></td>
    <td><%= bill.vat %></td>
    <td><%= bill.vatamt %></td>
    <td><%= bill.amount %></td>
    <td><%= bill.balance %></td>
    <td><%= link_to 'Show', bill %></td>
    <td><%= link_to 'Edit', edit_bill_path(bill) %></td>
    <td><%= link_to 'Destroy', bill, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Bill', new_bill_path %>
